{% extends 'base.html.twig' %}

{% block title %}{{ librairy.name }}{% endblock %}

{% block body %}
    <div class="container">
        <a href="{{ path('listing') }}" class="btn btn-secondary mt-2">retour</a>
        <h2 class="mt-4">Liste de livres de la bibliothèque : {{ librairy.name }}</h2>
        <ul class="list-group mt-3">
            {% for book in librairy.books %}
                <li class="list-group-item d-flex justify-content-between align-items-center">{{ book.name }}<a href="{{ path('book', { id: librairy.id, book: book.id }) }}" class="btn btn-primary">info livre</a></li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}
